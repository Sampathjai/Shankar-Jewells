{
  "name": "shanker-jewells-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Gold & Silver Jewellery Business Platform - Railway Production Release",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build && npm --prefix backend run build && cd backend && npx prisma generate && npx prisma db push && npx prisma db seed",
    "start": "cd backend && npm start"
  }
}

